      <%- include('header') %>  
      <% if (success && success.length > 0) { %>
        <div class="alert alert-info alert-dismissible fade show" role="alert">
          <strong style="text-align: center;"><%= success[0] %></strong> 
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
      <% } %>
    
      <% if (error && error.length > 0) { %>
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
          <strong style="text-align: center;"><%= error %></strong> 
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
      <% } %>
      <% if(complete && complete.length>0){ %>
      <div class="alert alert-info alert-dismissible fade show" role="alert">
        <strong style="text-align: center;"><%= complete %></strong> 
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
      </div>
      <% } %>
      <% if(sendemail && sendemail.length>0){ %>
        <div class="alert alert-info alert-dismissible fade show" role="alert">
          <strong style="text-align: center;"><%= sendemail %></strong> 
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
        <% } %>
          
      <!-- End Example Code -->
    </body>
  </html>